# 设计说明（DESIGN）

## 1. 总体架构

```
┌──────────────┐        ┌──────────────────────┐        ┌───────────────┐
│  Vue 3 前端   │  HTTP  │  Spring Boot 后端     │  JDBC  │   MySQL 8.0   │
│  (5173)      ├───────►│  (8080)              ├───────►│  用户/对局记录  │
│              │        │                      │        └───────────────┘
│  WebSocket   │        │   ┌───────────────┐  │
│  (长连接)     ├───────►│   │ 服务层         │  │  Redis │  ┌───────────────┐
└──────────────┘        │   │ Match/Game/   ├──┼───────►│  匹配队列/房间/  │
                        │   │ Auth/Ranking  │  │        │  排行榜/幂等标记 │
                        │   └───────────────┘  │        └───────────────┘
                        └──────────────────────┘
```

## 2. 各层职责

### 前端（Vue 3 + TS + Pinia）
- 登录 / 大厅 / 棋盘三个视图；`stores/game.ts` 维护 WebSocket 与对局状态。
- 点击消除只做「本地预判选中」，**最终合法性由后端裁决**；前端不计算得分/胜负，仅展示后端推送的权威状态。
- 通过 `ELIMINATE_SYNC` 收到对方操作后做闪烁动画（`byMe` 区分自己 / 对方，用不同颜色）。

### 后端（Spring Boot）
- REST：登录（BCrypt 校验 + 签发 token）、排行榜查询。
- WebSocket：匹配、走子、认输、断线/重连的实时消息分发；token 鉴权。
- 关键业务逻辑全部在后端：棋盘生成、BFS 路径校验、分数结算、幂等记账。

### MySQL（持久化真相）
- `users`：账号、BCrypt 密码哈希、胜/负/平、累计积分。
- `game_records`：最终对局记录；`room_id` 唯一约束作为幂等结算的**硬保证**。
- 自动建表：JPA `ddl-auto: update`；种子账号：`DataSeeder` 启动时幂等写入。

### Redis（实时状态 / 协调）
| Key | 类型 | 作用 |
|---|---|---|
| `match:queue` | List | 匹配队列（入队 / 出队匹配） |
| `room:{id}` | Hash | 房间权威状态：棋盘、双方、分数、状态、离线时间戳 |
| `user:room:{uid}` | String | 用户 → 房间反查，用于重连定位 |
| `rooms:active` | Set | 活跃房间集合，供定时扫描断线判负 |
| `settled:{roomId}` | String(SETNX) | 结算幂等标记 |
| `lock:room:{id}` | String(SETNX) | 房间级互斥锁，串行化走子 |
| `leaderboard` | ZSET | 排行榜（member=userId，score=累计积分） |
| `session:{token}` | String | 登录会话 token → userId |

**Redis 真正解决的问题**：① 匹配队列与房间成员/在线状态；② 未结束对局的临时状态
不依赖单进程内存（后端重启后，客户端重连仍能通过 Redis 恢复对局）；③ 排行榜实时排名。

## 3. 房间 / 对局状态机

```
匹配成功 ──► PLAYING ──┬── 棋盘清空 ──────────► FINISHED（结算 CLEAR）
                       ├── 一方认输 ──────────► FINISHED（结算 SURRENDER）
                       └── 断线 > 60s ────────► FINISHED（结算 DISCONNECT）
```

- 双方共享同一份棋盘，**无回合概念**：谁先点到合法的一对谁得分（竞速玩法）。
- 走子在房间级 Redis 锁内完成「读棋盘 → 校验 → 写回」，避免并发重复消除同一对。
- 死局处理：每次消除后若棋盘仍有余子但无解，自动洗牌剩余图案（保持成对、保证有解）。

## 4. 核心算法：连连看 BFS

`LinkSearchService.canConnect` 用「最少转弯数 BFS」。状态为 `(r, c, 方向)`，以转弯次数为代价
做广度优先，记录到达每个 `(r,c,方向)` 的最小转弯数；到达终点且转弯数 ≤ 2 即判定可连接。

- 复杂度 O(rows × cols × 4)，对 10×10（含边框）棋盘开销极小。
- 棋盘外围保留一圈空位（值为 0），天然支持「路径从棋盘外部绕行」的连连看规则。
- `findPath` 额外回溯出路径并压缩为折点，供前端连线动画（当前前端以闪烁展示，路径已随消息下发）。

## 5. 关键取舍

1. **不引入完整 Spring Security**，只用 `spring-security-crypto` 的 BCrypt：避免过滤器链复杂度，登录与 WS 鉴权用自签 token（Redis 会话）即可。
2. **幂等结算双保险**：Redis `SETNX` 做快速去重，MySQL `room_id` 唯一约束做最终保证；`settle()` 捕获唯一约束冲突直接返回，不重复记账。
3. **权威状态放 Redis、结果落 MySQL**：对局过程高频读写走 Redis，最终结算一次性写 MySQL，兼顾性能与持久化。
4. **断线重连**：断线仅在房间 Hash 记时间戳，60 秒宽限期内重连即恢复完整棋盘与分数；超时由定时任务判负结算，避免对局悬挂。
5. **同账号多开**：`userId → session` 单映射，后连覆盖先连；演示用两个不同账号，符合题意。